<%= link_to user_path(user), class:"card-product d-flex justify-content-evenly mt-3" do  %>
  <div class="card-go">➡</div>
  <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/skateboard.jpg" class="rounded-lg"/>
  <div class="card-product-infos d-flex flex-column justify-content-around align-items-center w-50 ">
    <h2 class="pt-3"><%= user.username %></h2>
    <p class="text-wrap text-break"><%= user.bio %></p>
    <div class="card-languages d-flex justify-content-between w-100">

      <div class="card-flags d-flex flex-column">
        <p class="m-0">Speaks:</p>
        <div class="languages d-flex">
          <% user.user_languages.each do |user_language| %>
            <%= render "shared/language_level_card", user_language: user_language%>
          <% end %>
        </div>
      </div>
     <div class="card-flags d-flex flex-column">
        <p class="m-0">Learning:</p>
        <div class="languages d-flex">
          <% user.user_languages.each do |user_language| %>
            <%= render "shared/language_level_card", user_language: user_language%>
          <% end %>
        </div>
      </div>
    </div>
  </div>

<% end %>
